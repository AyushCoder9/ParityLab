# ADR 0002: Optical Ledger interface

Status: accepted

The marketing surface uses cinematic event-braid storytelling. The guided simulation uses split dark/light fault space. The authenticated product uses a restrained instrument-panel interpretation. All three consume the same tokens and run-event model.
