# New chat bootstrap

Open `/Users/ayushkumarsingh/Documents/Codex/2026-07-18-here-is-the-stripe-internship-details/ParityLab` in Codex. Read `AGENTS.md`, `docs/PROJECT_BRIEF.md`, `docs/STATE.md`, `docs/HANDOFF.md`, `docs/MVP_BUILD_PLAN.md`, `docs/IMPLEMENTATION_PLAN.md`, `docs/PLAN.md`, every `docs/WORKSTREAMS/*.md`, and `docs/VERIFICATION.md` in that order. Inspect Git status and the latest commit before editing.

User intent: build ParityLab as a Stripe-specific, production-shaped flagship internship project. Product truth comes first: real backend/database/Stripe behavior, every visible route/control honestly wired, then award-level motion. Never present fixtures as live, never accept live Stripe keys, never expose secrets, and never claim a hosted or real-Stripe proof that has not run.

Current product truth:

- Durable PostgreSQL persistence, encrypted Stripe Sandbox connections, official Stripe SDK PaymentIntent vertical, separate durable worker/reference merchant, fifteen routed product screens, and premium scroll motion are implemented.
- Authentication and tenancy are real, not browser-preview state. Registration creates a user, organization/owner membership, project, default environments, session, and audit record atomically. Passwords use Argon2id; email is AES-GCM encrypted and queried through a keyed blind index.
- Sessions use opaque 24-hour cookies with only SHA-256 hashes in PostgreSQL. Cookies are `HttpOnly`, `SameSite=Lax`, and `Secure` by default. The insecure-cookie option is loopback-only. CORS is credentialed only for the configured web origin, cookie mutations enforce Origin-based CSRF protection, login returns generic failures, unknown accounts execute a dummy Argon2 check, and repeated failures are throttled.
- Runs/evidence/idempotency, Stripe connections/PaymentIntent execution, settings, environments, findings, notifications, and their audit records are project-scoped. Cross-tenant resource IDs return 404. Project settings, selected environment, finding resolve/reopen, notification read/read-all, and sanitized connection lists persist through restart.
- The web app restores the API session, protects product routes, displays server identity/project/resource counts, and uses real protected mutations. It never copies the session token to JavaScript storage. Seeded/public demo data remains visibly separate from tenant records.
- Migration `000006_auth_tenancy` adds encrypted/password user fields, project retention, hashed sessions, and environments. The authoritative isolated `tests/scripts/auth-resource-restart.sh` run passed, including cookie policy, registration sanitization, generic login failure, throttling, CSRF, protected Stripe execution, cross-tenant denial, expiration/revocation, restart persistence, and log-secret checks.
- Authenticated UI lint/unit/build/E2E-TypeScript checks pass. Against the fresh PostgreSQL/API/strict-Stripe-mock auth stack, `auth-product.spec.ts`, `auth-security.spec.ts`, and `state-boundaries.spec.ts` passed Chromium 17/17 in 11.6 seconds and WebKit 17/17 in 27.5 seconds; the dedicated stack and volumes were removed afterward.
- Migration `000007_webhook_correlation` and the dual-topic worker complete signed Stripe webhook correlation. Ingress stores only hashes plus a sanitized projection; the worker requires exact PaymentIntent/correlation metadata, derives tenant ownership from the run, and appends exactly one visible run event/report assertion. The isolated webhook restart contract passed for `run_000004`, including API and worker restarts, exact replay, changed-body conflict, safe ignored/unmatched outcomes, poison-job handling, and raw/secret absence.
- Long-lived SSE now reads bounded durable event batches, replays strictly after `Last-Event-ID`, preserves tenant/public boundaries, emits retry/heartbeat/completion frames, and stays open for later evidence. The isolated restart/reconnect contract replayed sequences 1–9, restarted the API, resumed with zero replay, and delivered webhook sequence 10 on the existing stream without duplication. Run detail uses credentialed native `EventSource` and visibly appends sequence-deduplicated evidence.

Still unfinished: no real Stripe account has been contacted; only the PaymentIntent duplicate-delivery scenario is connected through the full Stripe/worker/merchant vertical; hosted deployment, shared multi-instance throttling, richer identity lifecycle, and production operations remain. Resume at the first unfinished item in `docs/HANDOFF.md`; do not redo completed work or weaken the acceptance gates. Update `docs/STATE.md`, `docs/VERIFICATION.md`, and the workstream documents with exact evidence before pausing.
