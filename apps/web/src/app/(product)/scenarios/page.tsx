import type { Metadata } from "next";
import { ScenariosPage } from "@/components/product-pages";
export const metadata: Metadata = { title: "Failure scenarios" };
export default function Page() { return <ScenariosPage />; }
