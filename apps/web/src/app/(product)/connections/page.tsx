import type { Metadata } from "next";
import { ConnectionsPage } from "@/components/product-pages";
export const metadata: Metadata = { title: "Connections" };
export default function Page() { return <ConnectionsPage />; }
